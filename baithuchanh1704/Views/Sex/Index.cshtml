@model IEnumerable<baithuchanh1704.Models.Sex>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SexName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SexName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SexID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SexID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SexID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
